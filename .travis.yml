---

matrix:
  include:
    - dist: trusty
    - dist: xenial
    - dist: bionic

language: python
sudo: required

install:
  - which python
  - whereis python

  - pip install -U pip
  - pip install -U urllib3 pyOpenSSL ndg-httpsclient pyasn1

  # Install ansible
  - pip install -U ansible

  # Check ansible version
  - ansible --version

  # Create ansible.cfg with correct roles_path
  - printf '[defaults]\nroles_path=../' > ansible.cfg

  # dependencies
  - ansible-galaxy install lifeofguenter.oracle-java

script:
  - ansible-playbook tests/test.yml -i tests/inventory --connection=local -e "ansible_python_interpreter=$(which python)"

  - ansible-playbook tests/test.yml -i tests/inventory --connection=local -e "ansible_python_interpreter=$(which python)"

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
